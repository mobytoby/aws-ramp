# this is an auto generated file. This will be overwritten
query GetImageJob($id: ID!) {
  getImageJob(id: $id) {
    id
    name
    imageUrl
    processor
    isDone
  }
}
query ListImageJobs(
  $filter: ModelImageJobFilterInput
  $limit: Int
  $nextToken: String
) {
  listImageJobs(filter: $filter, limit: $limit, nextToken: $nextToken) {
    items {
      id
      name
      imageUrl
      processor
      isDone
    }
    nextToken
  }
}
